# Supabase (frontend)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=public-anon-key

# Optional: server-side AI analysis endpoint (Supabase Edge Function / other)
VITE_AI_ANALYSIS_URL=https://<project>.functions.supabase.co/analyze

# (Edge Function only) OpenAI key — set as a secret/environment variable for the function (DO NOT place in frontend env)
# OPENAI_API_KEY=sk-...
